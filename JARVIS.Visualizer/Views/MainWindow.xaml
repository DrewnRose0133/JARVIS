<Window x:Class="JARVIS.Visualizer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JARVIS Visualizer" Height="720" Width="1280" WindowState="Maximized" Background="Black" Foreground="LightBlue">
    <Grid>
        <TextBlock Name="ClockText" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0"/>
        <Image Name="StatusImage"
       Source="Assets/JARVIS.gif"
       Width="994" Height="522"
       HorizontalAlignment="Left"
       VerticalAlignment="Top"
       Margin="140,0,0,0"/>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,50,0">
            <TextBlock Text="Smart Home" FontSize="24" Margin="0,0,0,10"/>
            <Button Content="Light On" Click="LightOn_Click" Margin="0,5"/>
            <Button Content="Light Off" Click="LightOff_Click" Margin="0,5"/>
            <Button Content="Set AC to 72" Click="SetAC_Click" Margin="0,5"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="20">
            <TextBlock Text="Recent Alerts" FontSize="20"/>
            <ListBox Name="AlertsList" Height="120" Width="300"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20">
            <TextBlock Text="System Log" FontSize="20"/>
            <ListBox Name="LogList" Height="120" Width="300"/>
        </StackPanel>
    </Grid>
</Window>
